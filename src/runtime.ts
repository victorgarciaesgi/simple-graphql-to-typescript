import { generate } from './generateModel';
import path from 'path';
import { downloadSchema } from './getSchemas';
import fs from 'fs';

// export function sgtsGenerate({  }: { endpoint: string; json: string });
